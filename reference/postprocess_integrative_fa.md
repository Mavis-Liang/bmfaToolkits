# Post-process an integrative factor model fit

Post-process an integrative factor model fit

## Usage

``` r
postprocess_integrative_fa(method, fit, ...)
```

## Arguments

- method:

  Backend name.

- fit:

  A fit object from the corresponding `fit_*()` function.

- ...:

  Passed to the corresponding `postprocess_*()` function.
