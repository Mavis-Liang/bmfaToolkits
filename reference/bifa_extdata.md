# Example data shipped in extdata

This package ships example data files under `inst/extdata/`. Use the
loader functions below to access them.

## Details

Because these are stored in `extdata/`, they are not available via
[`data()`](https://rdrr.io/r/utils/data.html).
