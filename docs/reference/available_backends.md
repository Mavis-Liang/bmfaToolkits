# Check which backends are installed and usable

Many methods in the tutorial are implemented in external packages (often
on GitHub or Bioconductor). This helper reports availability and
provides install hints.

## Usage

``` r
available_backends()
```

## Value

A data.frame with backend name and availability.
