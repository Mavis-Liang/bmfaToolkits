# Load the toy dataset from this package

The toy datasetis shipped as an RDS under `inst/extdata/`.

## Usage

``` r
load_toy_data()
```

## Value

An object read by [`readRDS()`](https://rdrr.io/r/base/readRDS.html).
