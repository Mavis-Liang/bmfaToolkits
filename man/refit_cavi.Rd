% Generated by hand for this bundle
\name{refit_cavi}
\alias{refit_cavi}
\title{Refit CAVI after selecting the number of factors}
\usage{
refit_cavi(Y_list, fit0 = NULL, post_fit0 = NULL, cutoff = 0.05, ...)
}
\arguments{
\item{Y_list}{List of S study matrices (each \code{n_s x P}).}
\item{fit0}{Optional initial fit from \code{fit_cavi()}.}
\item{post_fit0}{Optional post-processed initial fit from \code{postprocess_cavi()}. If both \code{fit0} and \code{post_fit0} are supplied, \code{post_fit0} is used.}
\item{cutoff}{Eigen-proportion cutoff passed to \code{select_k_from_sigma()}.}
\item{...}{Passed to \code{fit_cavi()} for the refit.}
}
\value{Post-processed output of the refit (same structure as \code{postprocess_cavi()}).}
\description{
Implements a 2-step workflow: fit an initial CAVI model, post-process to obtain covariance components, select factor counts using an eigen-proportion rule, and refit CAVI with the selected \code{K} and \code{J_s}.
}
