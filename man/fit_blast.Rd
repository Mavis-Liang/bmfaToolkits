% Generated by hand for this bundle
\name{fit_blast}
\alias{fit_blast}
\title{Fit BLAST (script backend)}
\usage{
fit_blast(Y_list, k, q_s, n_MC = 10000, center = TRUE, scale = FALSE,\n  subsample_index = NULL, sample_outer_product = FALSE, ...)
}
\arguments{
\item{Y_list}{List of S study matrices (each n_s x P).}
\item{k}{Number of common factors.}
\item{q_s}{Study-specific factor counts, length 1 or length S.}
\item{n_MC}{Number of Monte Carlo iterations used by BLAST.}
\item{center}{Logical; center each study matrix before fitting.}
\item{scale}{Logical; scale each study matrix before fitting.}
\item{subsample_index}{Integer indices used by BLAST when \code{sample_outer_product = FALSE}.}
\item{sample_outer_product}{Logical; forwarded to BLAST.}
\item{...}{Passed to the BLAST implementation of \code{fit_blast()}.}
}
\value{A \code{bifa_fit} object with backend \code{"blast"}.}
\description{Wrapper that loads BLAST scripts from the package's bundled extdata (or the source tree under \code{inst/extdata/blast/}) and calls BLAST's \code{fit_blast()}.}
