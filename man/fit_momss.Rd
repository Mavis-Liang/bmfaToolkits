% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/momss.R
\name{fit_momss}
\alias{fit_momss}
\title{Fit MOM-SS (MOM-SS / BFR.BE backend)}
\usage{
fit_momss(
  Y_list,
  k,
  X = NULL,
  scaling = FALSE,
  center_within_study = TRUE,
  folds = 10,
  ...
)
}
\arguments{
\item{Y_list}{Optional list of study matrices. If provided, we create a stacked matrix.}

\item{k}{Positive integer number of shared factors.}

\item{X}{Optional covariate matrix or list of covariate matrices aligned to \code{Y_list}.}

\item{scaling}{Logical; whether to scale the data.}

\item{center_within_study}{Logical; whether to center each study's data before fitting. Recommended unless you have strong reasons not to.}

\item{folds}{Number of folds for cross-validation within MOM-SS.}

\item{...}{Parameters that can be pass directly to \code{BFR.BE::BFR.BE.EM.CV()}.}
}
\value{
A \code{bifa_fit} object.
}
\description{
This is a thin wrapper around \code{BFR.BE::BFR.BE.EM.CV()} (or similar entry points).
Because the upstream API may evolve, we keep this wrapper flexible:
\itemize{
\item If you pass \code{...} with \code{X} and \code{M} (the membership matrix), they are forwarded.
\item If you pass \code{Y_list}, we will stack it and construct \code{X} and \code{M} accordingly.
}
}
